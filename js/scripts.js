


/*This is to prevent the default refreshing of the page when keyboard Enter is
// clicked on the forms------------------------------------------------------*/
addDescription.addEventListener('submit', function(e){
  e.preventDefault();
});
userValue.addEventListener('submit', function(e){
  e.preventDefault();
});
